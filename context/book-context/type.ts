export type TBooksRes = {
  title: string;
  author: string;
  description: string;
  userId: string;
  $id: string;
  $createdAt: string;
  $updatedAt: string;
  $permissions: Array<string>;
  $databaseId: string;
  $collectionId: string;
};
